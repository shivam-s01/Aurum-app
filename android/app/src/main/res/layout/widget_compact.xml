<?xml version="1.0" encoding="utf-8"?>
<!--
  MINIMAL DIAGNOSTIC LAYOUT — plain solid background, two TextViews,
  no ImageView bitmaps, no custom shape drawables beyond a flat color.
  Used to isolate whether the widget crash is layout/resource related.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root_tap"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#1A1A2E"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Aurum"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/widget_artist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Loading"
        android:textColor="#AAAAAA"
        android:textSize="11sp"
        android:layout_marginTop="4dp"/>

</LinearLayout>
